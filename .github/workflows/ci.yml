name: CI

on:
  push:
    branches: ["**"]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        run: pip install uv

      - name: Install dependencies
        run: uv pip install --system -e .

      - name: Install ruff and mypy
        run: uv pip install --system ruff mypy

      - name: Ruff format
        run: ruff format --check .

      - name: Ruff linter
        run: ruff check .

      - name: Mypy
        run: mypy core

      - name: Unit tests
        run: pytest core/tests/unit

      - name: Integration tests
        run: pytest core/tests/integration

      - name: BDD tests
        run: pytest core/tests/behaviour